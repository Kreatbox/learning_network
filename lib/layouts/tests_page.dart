import 'package:flutter/material.dart';

class TestsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('التدريبات'),
      ),
      body: Center(
        child: Text('هذه هي صفحة التدريبات'),
      ),
    );
  }
}
